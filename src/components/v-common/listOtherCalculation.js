class ListOtherCalculation {
  constructor (idCalc, name, condition, size, firstPrice, secondPrice) {
    this.idCalc = idCalc
    this.name = name
    this.size = size
    this.condition = condition
    this.firstPrice = firstPrice
    this.secondPrice = secondPrice
  }
}
module.exports = ListOtherCalculation
